import React from 'react'
import {appleImg} from "@/app/utils"

function Navbar() {
  return (
    <header>
        <nav>
            <Image src={appleImg}  alt="Apple" width={14} height={18} />
        </nav>
        <div>

        </div>
    </header>
  )
}

export default Navbar;