import React from 'react'

function ModelView({ index,groupRef,gsapType,controlRef,setRotationState,item,size}){

  console.log(`I am the component props ll; ${props.size}`)
  return (
   <section>
        ModelView
   </section>
  )
}

export default ModelView